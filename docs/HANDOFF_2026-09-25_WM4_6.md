# SIMCLONE — HANDOFF
## WM4.6 wood ecology candidate + live RP1 repair

วันที่ส่งต่อ: 25 กันยายน 2026 18:05 +07  
บทบาทแชทถัดไป: **Project Brain + Lead Developer**

Repo: https://github.com/nustanakritwithai/Simclone  
Live: https://nustanakritwithai.github.io/Simclone/  
Handoff เดิมที่ยังใช้เป็นบริบทแต่ ecology ส่วนนั้น **ล้าสมัย**: `docs/HANDOFF_2026-09-25_CLOSEOUT.md`

อย่าเชื่อ handoff นี้แทน source จริง

---

## 0. สถานะหนึ่งบรรคัฐ

- `main` ล่าสุดที่ตรวจตอนเขียน: `9799a6ac54cb587384475ba9be7389e72f05b525`
- บน `main` โค้ด writer คือ **WM4.5 food ecology + wood แบบ K6 เดิม**
- **WM4.6 ยังไม่ได้อยู่บน `main`**
- candidate: PR #65 `feature/wm4.6-wood-ecology` @ `2c11cfdaa82f1710034baa3f2ca25f0d92803d28`
- PR #66 ซ่อมวงจร Axe/Hammer **merged แล้ว** เข้า `main` ที่ `9799a6ac`
- Verify ของหัว #65 ตอนเขียนยัง **in_progress** → UNKNOWN ไม่ใช่ PASS
- #65 `mergeable_state=dirty` เพราะ `main` เดินหลัง merge #66

ห้าม merge #65 จนกว่าจะ rebase ทับ `main` ล่าสุด แล้ว exact candidate Verify ของหัวใหม่เป็น SAT และ exact merged-main Pages เป็น SAT

---

## 1. กฎก่อนเริ่มงาน

เริ่มทุกครั้งด้วย:

1. ตรวจ SHA ของ `main` อีกครั้ง — ค่าในไฟล์นี้อาจเก่าแล้ว
2. ตรวจ Actions / Pages ของ exact commit
3. ตรวจ PR ที่เปิดอยู่
4. อ่าน `AGENTS.md`, `GAME_PLAN.md`, `docs/STATUS.md`, `docs/NEXT_STEPS.md`
5. อ่านไฟล์ authority จริง: `src/worldsim-resource-authority.mjs`
6. VIP: Verify → Improve → Prove
7. UNKNOWN ห้ามนับเป็น PASS
8. ห้าม force push / ห้าม merge stale branch

Simulation authority:

- ห้าม `Math.random` ใน simulation
- ห้าม wall-clock / `Date` เป็นกฎเกม
- ห้าม DOM ใน engine
- UI ส่ง validated command เท่านั้น
- ห้ามสร้าง resource writer / production executor ตัวที่สอง

---

## 2. ความจริงของเอกสาร vs โค้ด

ไฟล์บน `main` ที่ยังพูดว่า “WM4.1 writer / ยังไม่มีสูตรอาหาร ecology” **ไม่ตรงกับโค้ดปัจจุบัน**

| ชั้น | ความจริงตอนเขียน handoff นี้ |
|---|---|
| `README.md` บน `main` | ยังเขียน WM4.1 / WM4.4 evidence-only |
| `docs/HANDOFF_2026-09-25_CLOSEOUT.md` | ยังเขียน WM4.1 / ห้ามเปิดสูตรอาหาร |
| `src/worldsim-resource-authority.mjs` บน `main@9799a6ac` | `RESOURCE_REGEN_AUTHORITY_VERSION='wm4.5-food-ecology-1'` |
| Food บน `main` | `wm4.5-conservative-v1` active |
| Wood บน `main` | ยัง `+1` / 720 ตาม K6 ไม่ดู ecology |
| Stone | finite ทั้ง `main` และ candidate |

อย่าเปิดสูตรอาหารใหม่ และอย่าย้อน WM4.5 กลับไปเลือก Formula Lab อีกรอบ

---

## 3. สิ่งที่ถือว่าอยู่ใน `main` แล้ว

เส้นปล่อยที่ตรวจจาก commit จริง ไม่ใช่จาก README:

- Knowledge Continuity 1
- Rust Survival RS1–RS4
- Production Planning RP1 (opt-in coordinator)
- Mentorship KF1
- WorldSim WM4.5 food ecology authority
- Modular wooden building pieces (Foundation / Wall / Doorway / Roof) merge ที่ `3096da3b`
- RP1 liveness repair จาก PR #66 merge ที่ `9799a6ac`

หลักฐาน #66 ที่อ้างใน merge message:

- exact candidate `447204f87a44e0778501ea0067882da9b4a8d8d4`
- Verify #380 SAT
- ซ่อมวงจร equip Axe/Hammer ที่ reproduce ได้บนไซต์จริง
- ไม่เปลี่ยน save schema และไม่เปลี่ยน resource authority

Pages ของ exact `main@9799a6ac` ต้องตรวจใหม่ตอนรับงาน — handoff นี้ไม่นับ Pages ให้

---

## 4. Candidate ที่ค้าง: PR #65 WM4.6

URL: https://github.com/nustanakritwithai/Simclone/pull/65  
Branch: `feature/wm4.6-wood-ecology`  
หัวตอนเขียน: `2c11cfdaa82f1710034baa3f2ca25f0d92803d28`  
สัญญา: `docs/WORLDSIM_WM4_6_WOOD_ECOLOGY.md`

### พฤติกรรมที่ใส่แล้วในสาขา

Writer ยังตัวเดียว: `applyWorldResourceRegeneration()`

- Food คง `wm4.5-conservative-v1`
- Wood ใช้ `woodYieldPotential`
  - `< 0.08` → `0`
  - นอกนั้น → `+1` ทุก 720 ticks
  - cap ที่ `node.max`
- Stone ไม่งอก
- ลำดับเขียนยังเป็น food แล้ว wood
- cache ecology แยก `food` / `wood`
- ถ้าไม่ส่ง `woodMode` จะตาม `foodMode` ดังนั้น `step(..., {resourceRegenerationMode})` เลือกทั้งคู่โดยไม่ต้องแก้ `engine.mjs`

ข้อความท้าย PR ที่ยังสั่ง “แก้ engine.mjs” **ล้าสมัยแล้ว** อย่าทำซ้ำ

### ไฟล์สำคัญในสาขา

- `src/worldsim-resource-authority.mjs`
- `scripts/worldsim-wood-ecology-authority-proof.mjs`
- `tests/worldsim-resource-authority.test.mjs` และ test ที่อ้าง writer string `wm4.6`
- `docs/WORLDSIM_WM4_6_WOOD_ECOLOGY.md`
- `docs/STATUS.md` / `docs/NEXT_STEPS.md` / `README.md` ถูกเลื่อนไปพูด WM4.6 แล้วบนสาขานี้เท่านั้น
- พับซ่อม #66 เข้าสาขาแล้ว:
  - `src/production-planning.mjs` (`equipForWork`)
  - `tests/production-liveness.test.mjs`
- pin ที่สร้างจากซอร์สหลังพับ:
  - `production-planning.mjs` → `847f11e428eb3db0`
  - `worldsim-resource-authority.mjs` → `1c28b61f4e773697`

หลัง rebase ห้ามใช้ pin ชุดนี้ต่อโดยไม่รัน `node scripts/pin-assets.mjs` ใหม่ทั้งต้นไม้

---

## 5. งานแรกของแชทถัดไป — ปิด #65 ให้ถูกวิธี

อย่าเริ่ม WM4.7 และอย่าเพิ่ม content

1. ตรวจ `main` SHA อีกครั้ง
2. rebase / merge `main@9799a6ac` (หรือใหม่กว่า) เข้า `feature/wm4.6-wood-ecology`
3. คาดว่าชนที่ `index.html` และอาจชน `src/production-planning.mjs` เพราะสาขานี้มีสำเนาซ่อม #66 อยู่แล้ว
4. คง logic `equipForWork` ของ #66 ไว้
5. คง logic wood ecology ของ WM4.6 ไว้
6. รัน `node scripts/pin-assets.mjs` จากต้นไม้หลัง resolve
7. อย่าคัด `index.html` เก่ามาทับ
8. ตรวจ unit ที่เกี่ยวข้องอย่างน้อย:
   - `tests/worldsim-resource-authority.test.mjs`
   - `tests/production-planning.test.mjs`
   - `tests/production-liveness.test.mjs`
   - `node scripts/worldsim-wood-ecology-authority-proof.mjs`
   - `node scripts/worldsim-food-ecology-authority-proof.mjs`
9. รอ exact candidate Verify ของหัวใหม่
10. merge ได้เฉพาะเมื่อ Verify SAT
11. รอ exact merged-main Pages SAT
12. ค่อยอัปเดต README / STATUS บน `main` ให้ตรงโค้ด

ถ้า Verify fail ห้ามปิดด้วยการลด proof

---

## 6. PR ที่ห้ามยุ่งแบบ merge ค้าง

| PR | สถานะ | คำสั่ง |
|---|---|---|
| #65 | เปิด, dirty, candidate WM4.6 | งานปัจจุบัน ต้อง rebase |
| #66 | merged | อย่าเปิดใหม่ |
| #63 | เปิด แต่ฐานเก่ากว่า `main` | ห้าม merge stale |
| #61 | เปิด แต่ฐานเก่ากว่า `main` | ห้าม merge stale |

สาขาอ้างอิงเก่าใช้เป็น donor ได้ ห้าม merge ทับ `main`

---

## 7. ลำดับหลัง #65 ปิดจริง

ปิดจริง = อยู่บน `main` และ Pages ของ exact main SAT

ถัดไปทีละ gate:

1. WM4.7 harvest-pressure — ยังไม่เริ่ม
2. WM4.8 shadow resource-zone placement
3. ค่อยเปิด Kingdom จาก observation → authority ทีละชิ้น
4. Knowledge continuity ข้ามรุ่นที่ยังไม่ครบ
5. V0.6 multi-step goal
6. V1.0 Original-only autonomous proof

วงจรที่ต้องทำให้เกิดจริง ไม่ใช่เพิ่มของ:

`เห็นโลก → รู้ไม่เท่ากัน → ตัดสินใจต่างกัน → เรียนรู้ → ถ่ายทอด → แบ่งงาน → สร้างสังคม → คนตาย → ความรู้ยังอยู่ → Generation ใหม่เปลี่ยนโลก`

---

## 8. Definition of Done ของ WM4.6

ถือว่า DONE เมื่อครบทุกข้อ:

- wood ecology อยู่ใน writer เดิมเพียงตัวเดียว
- food สูตร WM4.5 ไม่ถูกเปิดใหม่
- stone ยัง finite
- ไม่มี node spawn / harvest-pressure / writer ที่สอง
- A/B `legacy` ยังใช้เทียบได้
- replay / save-load ของโหมด ecology และ legacy ผ่าน
- exact candidate Verify SAT
- exact merged-main Pages SAT
- README / STATUS / NEXT_STEPS บน `main` ตรงกับโค้ด
- UNKNOWN ไม่ถูกนับเป็น PASS

---

## 9. สิ่งที่แชทนี้ทำแล้ว และสิ่งที่ยังไม่ทำ

ทำแล้ว:

- เขียนสัญญา WM4.6
- ใส่ wood ecology ใน writer เดิม
- ให้ `woodMode` ตาม `foodMode` ถ้าไม่ส่งมา
- เพิ่ม proof script และอัปเดต docs บนสาขา candidate
- พับซ่อม RP1 จาก #66 เข้าสาขา #65 และ pin ใหม่ครั้งหนึ่ง

ยังไม่ทำ / ห้าม pretends ว่าทำแล้ว:

- rebase #65 ทับ `main@9799a6ac`
- Verify SAT ของหัว `2c11cfda`
- merge #65
- Pages SAT หลัง merge WM4.6
- แก้ README บน `main` ให้เลิกพูด WM4.1
- WM4.7+

---

## 10. คำสั่งรับงานสั้น ๆ

```
รับงานจาก docs/HANDOFF_2026-09-25_WM4_6.md
ตรวจ main SHA ก่อนเชื่อตัวเลขในไฟล์
rebase PR #65 ทับ main ล่าสุด
รัน pin-assets จากต้นไม้หลัง resolve
อย่า merge จนกว่า exact candidate Verify และ exact main Pages จะ SAT
UNKNOWN is not PASS
อย่าเริ่ม WM4.7
อย่าเลือกสูตรอาหารใหม่
```
